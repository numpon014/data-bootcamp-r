# vectorization
gpas <- c(toy = 3.41, bew = 3.95, john = 2.58)
gpas

# add 0.01 to each element
gpas + 0.01

# subtract 0.05 from each element
gpas - 0.05

# vectorization with numeric vector
numbers <- c(5, 10, 15)
numbers * 2
numbers + 2